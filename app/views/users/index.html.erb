<h1>Users index</h1>
<% if @users.any? %>
    <div class="row">
        <% @users.each do |user| %>
            <div class="col-sm-3 px-3 pb-3">
                <div class="card">
                    <img class="card-img-top" src="<%= gravatar_url(user, size: 300) %>" alt="">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= user.name%>
                        </h5>
                        <p class="text-muted">Joined at <%= user.created_at.strftime("%Y-%m-%d") %></p>
                        <%= link_to "show detail", user_path(user), class: "btn btn-secondary btn-block" %>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
<% end %>

<%= paginate @users%>